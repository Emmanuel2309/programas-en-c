#include <stdio.h>

/* Promedio curso.
El programa, al recibir como dato el promedio de un alumno en un curso
universitario, escribe aprobado si su promedio es mayor o igual a 6.
PRO: variable de tipo real. */
void main(void)
{
    float PRO; // Declaración de la variable PRO de tipo real (número decimal)
    
    printf("Ingrese el promedio del alumno: "); // Muestra un mensaje para que el usuario ingrese el promedio
    scanf("%f", &PRO); // Lee el promedio ingresado por el usuario y lo almacena en la variable PRO
    
    if (PRO >= 6) // Comprueba si el promedio es mayor o igual a 6
    {
        printf("\nAprobado"); // Si es mayor o igual a 6, muestra "Aprobado" en pantalla
    }
}
