#include <stdio.h>

/* Incremento de precio.
El programa, al recibir como dato el precio de un producto importado, 
incrementa 11% el mismo si éste es inferior a $1,500. 
PRE y NPR: variable de tipo real. */
void main(void)
{
    float PRE, NPR; // Declaración de las variables PRE y NPR de tipo real (número decimal)
    
    printf("Ingrese el precio del producto: "); // Muestra un mensaje para que el usuario ingrese el precio
    scanf("%f", &PRE); // Lee el precio ingresado por el usuario y lo almacena en la variable PRE
    
    if (PRE > 1500) // Comprueba si el precio es mayor a $1,500
    {
        NPR = PRE * 1.11; // Calcula el nuevo precio con el incremento del 11%
        printf("\nNuevo precio: %7.2f", NPR); // Muestra el nuevo precio en pantalla con dos decimales
    }
}
