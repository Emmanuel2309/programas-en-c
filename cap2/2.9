#include <stdio.h>
#include <math.h>

/* Expresión.
El programa, al recibir como datos tres valores enteros, establece si los
mismos satisfacen una expresión determinada.
R, T y Q: variables de tipo entero.
RES: variable de tipo real. */
void main(void)
{
    float RES; // Declaración de la variable RES de tipo real (número decimal)
    int R, T, Q; // Declaración de las variables R, T y Q de tipo entero
    
    printf("Ingrese los valores de R, T y Q: "); // Muestra un mensaje para que el usuario ingrese los valores de R, T y Q
    scanf("%d %d %d", &R, &T, &Q); // Lee los valores ingresados por el usuario y los almacena en las variables R, T y Q
    
    RES = pow(R, 4) - pow(T, 3) + 4 * pow(Q, 2); // Calcula el resultado de la expresión matemática y lo almacena en la variable RES
    
    if (RES < 820) // Comprueba si el resultado de la expresión es menor que 820
        printf("\nR = %d\tT = %d\tQ = %d", R, T, Q); // Si se cumple la condición, muestra en pantalla los valores de R, T y Q
}
